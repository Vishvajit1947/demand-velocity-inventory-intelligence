# raw M5 data (too large for GitHub) — keep calendar.csv (small, needed at runtime)
data/raw/*
!data/raw/calendar.csv
# private documents
official_docs/
# python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
# node / frontend
node_modules/
frontend/dist/
# env / secrets
.env
*.local