# Copy this file to .env and set the value for your environment.
# Point at the mock server (MT-25) during frontend development, or at the real backend for integration.
VITE_API_BASE=http://localhost:8000
